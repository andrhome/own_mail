1. getMessages(method, url, mode)
2. createMessagesList(obj, insertBlock, mode)
3. showMessage(showBlock, openedBlock)
4. hideMessage(showBlock, openedBlock)
5. renderTemplate(template, insertBlock, place)